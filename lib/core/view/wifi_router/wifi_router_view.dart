import 'package:flutter/material.dart';

class WifiRouterView extends StatelessWidget {
  const WifiRouterView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}